# Screenshots

`01-browse.jpg` — the Browse view in dark theme — is the one used in the main `README.md`.

## Adding more

Drop a file in here and reference it from `README.md` as
`![alt text](docs/screenshots/your-file.jpg)`.

Worth capturing if you want to expand it:

| Suggested name | What to capture | Where |
|---|---|---|
| `02-scoreboard.jpg` | The parallel-coordinates chart and metrics table | **Scoreboard** |
| `03-latent-space.jpg` | The 3D point cloud with the red `p_u` vector | **Collaborative → Matrix factorisation** |
| `04-drawer.jpg` | The film drawer showing the per-term maths | Click any poster → **Content** tab |
| `05-reel.jpg` | One reel slide — rank numeral, synopsis, three score bars | **Browse → Play** |

## Tips

- Capture at **1440 × 900** or wider so the sidebar and content both fit.
- Leave **Show scores & maths** *on* — the numbers are the point of the project.
- Export as **JPEG at ~90%**, not PNG. These are poster-heavy shots: the same image is 263 KB as
  JPEG versus 848 KB as PNG, with no visible difference at README width.
- On Windows: `Win + Shift + S` for a region grab, or `Win + PrtScn` for the full screen.
